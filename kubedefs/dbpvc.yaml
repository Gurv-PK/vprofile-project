apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc-claim
  labels:
    app: vprodb
 
spec:
  resources:
    requests:
      storage: 3Gi
  storageClassName: default
  accessModes:
    - ReadWriteOnce
